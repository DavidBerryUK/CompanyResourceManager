export enum EnumListComponentStyle {
    htmlList,
    list,
    wordsCsv,
    wordsCsvWithAnd,
}

export enum EnumListCompomentTitle {
    none,
    simple,
    listHeader,
}

export enum EnumListComponentValueDisplay {
    none,
    checkbox,
    switch,
    text,
}

export enum EnumListTextFilter {
    none,
    inHeader,
    belowHeader,
}
